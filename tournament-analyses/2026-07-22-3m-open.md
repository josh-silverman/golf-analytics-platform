# 3M Open — Evidence-First Model Report

- **Event:** 3M Open, TPC Twin Cities — starts 2026-07-23
- **Field:** 144 players (all DataGolf-covered)
- **Pulled as-of:** 2026-07-22
- **Sources:** served board (`path_a@d69cf2a7323f`), DataGolf raw pre-tournament (`baseline_history_fit`, live), sportsbook consensus (de-vigged to DataGolf's field totals), DataGolf skill ratings + decompositions.

> **DataGolf's per-player values removed 2026-08-25.** Their terms are personal
> use only, no redistribution, and this repository is public (see
> [ledger.md](../docs/ledger.md) §2.8). **Our own served-board numbers are kept**,
> because they are this project's output rather than DataGolf's, and the central
> finding below is a ratio between the two, which survives the redaction intact.

---

## ⚠️ Critical finding first — read before using any number

**The live product is not serving what it claims to serve this week.** Traced end to end:

- The **served board shows Scheffler at 2.24%** to win. DataGolf rates him roughly **10x higher**. The entire served win market spans only 0.6% to 2.2%, which is the collapse.
- Root cause is a code bug, not calibration: `CachingProviderWrapper` (the production provider) implements `get_pretournament_preds` but **not** `get_pretournament_full_preds`, the five-market method Path A serving depends on. With no override and no `__getattr__`, that call hits the base-class default returning `{}`. Its own docstring states the consequence: *"Path A cold-starts every player to the SG-only model on a provider that doesn't override this."*
- **Consequence:** every player silently falls back to the **v2 SG-only model**, whose win market is the known flat/collapsed one. Path A's DataGolf-direct serving is inert in production. The same collapse was confirmed on the prior week's live Open board (McIlroy served at 2.28%).

**Implication for this report:** the "served" numbers are the v2 model, and its **win output is unusable**. Win / value / top-20 / make-cut picks below are built from **DataGolf's raw probabilities** (what Path A intends to serve, readable via the unwrapped provider), with the served v2 values shown alongside to expose where the live board diverges. The bug was **not** fixed as part of this analysis; it was flagged for a separate task.

> Note: the forward out-of-sample track record grades these same served boards, so those skill numbers reflect the v2 model, not DataGolf-direct. Real, but mislabeled "Path A" until this bug is fixed.

---

## Top 5 Win Picks — from DataGolf raw (intended output)

In model order, with our own served value alongside:

| # | Player | Live site shows (v2, ours) | DataGolf vs de-vigged market |
|---|--------|----------------------------|------------------------------|
| 1 | **Scottie Scheffler** | 2.24% (broken) | model higher than the sharps |
| 2 | Maverick McNealy | 0.97% | model higher |
| 3 | Kurt Kitayama | 1.22% | model higher |
| 4 | Tom Kim | 0.82% | model higher |
| 5 | Hideki Matsuyama | 0.67% | model higher |

Scheffler is a **generational outlier in this field**: his projected SG is roughly 2.3x the next-best player's, and DataGolf's win probability for him is nearly 7x the number two. DataGolf rates him **above** the fair market, so the model is *more* bullish than the sharps.

## Best Value Plays — DataGolf above the de-vigged market (Top-20 market, the validated-skill lane)

Ranked by the size of the model-over-market gap:

| Player | Rank by edge | Feature driver |
|--------|--------------|----------------|
| **Scheffler** | 1 | far and away the best SG total, dominant across the board |
| **Tom Kim** | 2 | approach-led, positive course fit |
| **Maverick McNealy** | 3 | balanced SG total, best putting of the leaders |
| **Doug Ghim** | 4 | strongest course history in the field |
| **Ben Kohles** | 5 | modest raw SG but **the top course-fit adjustment in the field** |

The market here *excludes* DataGolf's baseline, so these are genuine model-versus-sharps divergences. Per the project's documented reliability, top-20 and make-cut are the markets with real skill, so value is sourced there, **not** win/top-5.

## Best Top-20 Picks — DataGolf raw leaders

In model order: Scheffler · **McNealy** · Kitayama · Kim · Matsuyama · Ghim · Koivun · Conners · Meissner · Kohles.

**Live-board divergence:** the served v2 model badly under-rates several of these relative to DataGolf. McNealy is served at **24.9%**, Kim at 22.8%, Koivun at 21.6%, in each case well below DataGolf's read. The v2 model compresses toward the field mean; DataGolf separates the second tier much more sharply.

## Safest Make-Cut Picks — DataGolf raw

In model order: Scheffler · McNealy · Kitayama · Tom Kim · Matsuyama · Ghim · Koivun · Conners.

## Biggest Fades — market rates them for Top-20, DataGolf doesn't

Ranked by the size of the market-over-model gap:

| Player | Rank by gap | Why DG fades |
|--------|-------------|--------------|
| **Gordon Sargent** | 1 | SG total well below the field |
| Rasmus Højgaard | 2 | barely positive SG total |
| Jake Knapp | 3 | long off the tee, but DG's distance adjustment is negative here |
| Stephan Jaeger | 4 | barely positive SG total; market pays for name, DG for form |
| Emiliano Grillo | 5 | barely positive SG total |

## Model Insights

1. **The serving bug is the story of the week.** The live board is the v2 SG model, not DataGolf, and win is collapsed by roughly 10x. The leaderboard win column and betting-edge win rows are unreliable until `get_pretournament_full_preds` is forwarded through the caching wrapper.
2. **Scheffler is a historic mismatch for this field.** His projected SG is roughly 7x the 20th-best player's. DataGolf is even higher on him than the sharp market, the rare case where "fade the chalk" has no support.
3. **TPC Twin Cities course-fit is doing real work in the second tier.** DataGolf's decomposition surfaces fit as the driver for value names with unremarkable raw SG: **Ben Kohles**, **Zac Blair**, and Christiaan Bezuidenhout. **Doug Ghim's** edge is course *history*, the highest among contenders, and it is the one place the broken live model lands bullish for a defensible reason (served make-cut 86%, well above DataGolf's read).
4. **Bombers are not being rewarded here.** Keith Mitchell, Kitayama and Knapp are among the field's longest drivers, yet DataGolf's driving-distance adjustment is **negative** for all three. The model reads the course as one where distance doesn't convert, a genuine anti-narrative signal.
5. **Where the model agrees with consensus:** the top of the board. DataGolf and the sharp market rank the same five or six names in the same order across every market. Disagreement concentrates in the middle of the top-20 band, which is exactly where edges are findable.

## Methodology Notes

- **What the model evaluates:** five nested markets (win ⊆ top-5 ⊆ top-10 ⊆ top-20 ⊆ make-cut), each a probability, forced coherent (running-max) and field-normalized (win → 1, top-5 → 5, …). Underlying signal is strokes-gained: per-category skill (off-tee / approach / around-green / putting), field-relative margins, recent-form index, round-to-round volatility.
- **Intended vs actual source:** Path A is designed to serve DataGolf's `baseline_history_fit` directly to covered players (all 144 here) and use the v2 SG model only for cold-starts. Due to the bug above, actual serving = v2 model for everyone. DataGolf's own model exposes feature contributions via its decomposition (course history, course fit, driving distance / accuracy, age, timing); the v2 model's contributions are its SG-category and field-relative features.
- **Market comparison:** sportsbook consensus (DataGolf's own baseline excluded), de-vigged by scaling each market to its true field total, so edge = model prob − fair implied.
- **Confidence metrics available but not tabled:** DataGolf `std_deviation` + `sample_size` per player; the v2 model's `round_count` + `score_volatility`.

---

**Data provenance:** all figures trace to a 144-player pull covering the five markets, served + DataGolf-raw + market + skill ratings + decompositions. DataGolf's half of that pull is held privately; see the redaction note at the top.
